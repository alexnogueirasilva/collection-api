meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@system.com",
    "password": "Admin123!"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
